{
  "name": "progression-to-midi",
  "version": "1.0.0",
  "description": "Generates MIDI from chord progressions",
  "main": "dist/bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild Main.ts --bundle --outfile=dist/bundle.js --platform=browser --format=iife --sourcemap",
    "watch": "esbuild Main.ts --bundle --outfile=dist/bundle.js --platform=browser --format=iife --sourcemap --watch"
  },
  "keywords": [
    "midi",
    "chord",
    "generator"
  ],
  "author": "1legality",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.14.1",
    "esbuild": "^0.25.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "jspdf": "^3.0.1",
    "midi-writer-js": "^3.1.1"
  }
}
